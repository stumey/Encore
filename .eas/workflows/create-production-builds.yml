name: Create Production Builds


on:
  push:
    tags:
      - 'v*' # Triggers for tags starting with 'v', e.g., v1.0.0, v2.1

  jobs:
    build_android:
      type: build
      params:
        platform: android
    build_ios:
      type: build
